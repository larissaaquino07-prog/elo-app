<!-- Composition root placeholder — ARCHITECTURE.md §2.3. `container.ts`
     lands here: the single place every concrete repository/engine/mapper
     is constructed and wired into use cases and stores. Not implemented
     yet — depends on macro-stages 3-4 (repositories) existing first. -->
