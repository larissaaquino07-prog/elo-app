import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function MemoryScreen() {
  return (
    <PlaceholderScreen
      title="Memory"
      description="Searchable history and vocabulary/mistake tracking depend on the memory extraction engine (ARCHITECTURE.md §5.2, macro-stages 9–10) — not built yet."
    />
  );
}
