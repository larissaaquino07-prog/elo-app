import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function ProgressScreen() {
  return (
    <PlaceholderScreen
      title="Progress"
      description="Fluency/confidence scores, streaks, and trend cards (DESIGN_SYSTEM.md §5.2) land in macro-stages 13–20 of IMPLEMENTATION_PLAN.md, once real session data exists to show."
    />
  );
}
