<!-- SyncCoordinator — ARCHITECTURE.md §4: idempotent upsert (ADR-007),
     per-device watermark, hybrid realtime+pull sync. Not implemented yet —
     macro-stage 4 (schema/cursor) and later stages (real sync logic). -->
