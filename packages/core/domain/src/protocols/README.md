<!-- Domain-layer protocols (TS interfaces) placeholder — ARCHITECTURE.md
     §2.1: SessionRepository, MemoryRepository, SyncCoordinating, plus the
     Engine protocols (ConversationEngine, VoiceEngine, MemoryExtractionEngine,
     ADR-002/ADR-005). Implemented only in packages/core/data — this package
     never imports an implementation, only defines the shape. Not implemented
     yet — macro-stage 3.5 establishes the folder, first protocols land in
     macro-stage 5+. -->
