<!-- Domain entities placeholder — ARCHITECTURE.md §9.2 (pure TS types: Session,
     Mistake, VocabularyItem, Topic, LearnerProfile, etc.), framework-free.
     Not implemented yet — first entities land alongside macro-stage 4's
     Drizzle schema (the Domain entity and the Data row shape are mapped by
     a Mapper, ARCHITECTURE.md §2.1, never the same type). -->
