<!-- Use cases placeholder — ARCHITECTURE.md §2.2's enumerated initial set:
     StartSessionUseCase, EndSessionUseCase, GetAdaptiveFocusUseCase,
     SyncMemoryUseCase, SearchMemoryUseCase, ExportUserDataUseCase,
     DeleteUserDataUseCase. Each takes repository/engine dependencies via
     constructor injection (ADR-012). Not implemented yet — macro-stage 9+. -->
